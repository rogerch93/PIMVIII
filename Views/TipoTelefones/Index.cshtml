@model IEnumerable<PimViii.Models.TipoTelefone>

@{
    ViewData["Title"] = "Index";
}

<h1>Tipo de Telefone</h1>

<p>
    <a asp-action="Create">Gerar novo item na tabela</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tipo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pessoa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tipo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pessoa.NomePessoa)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoTelefoneId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TipoTelefoneId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TipoTelefoneId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
